import { Container } from '@mui/material';

const Gallery = () => {
    return (
        <Container sx={{ py: 4 }}>
            <h1 className="text-center">Gallery</h1>
        </Container>
    );
};

export default Gallery;
