import { Container } from '@mui/material';

const Home = () => {
    return (
        <Container sx={{ py: 4 }}>
            <h1 className="text-center">Howdy</h1>
        </Container>
    );
};

export default Home;
